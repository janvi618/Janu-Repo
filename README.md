# SignalFoundry

A polished, responsive front-end concept for an AI vendor scouting and assessment program. The experience turns the program charter into an operational dashboard with portfolio metrics, pipeline tracking, risk signals, category filtering, and vendor intake.

## Run locally

```bash
python3 -m http.server 4173
```

Then open `http://localhost:4173`.

The app has no build step or external dependencies.
