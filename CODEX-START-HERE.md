# Start Codex here: Category Pulse integration

The original application is restored under `category-pulse/`. It is already extracted. Work in that directory; the loose files at repository root are earlier uploads and are not the application layout.

## Start a fresh task

Select repository `janvi618/Janu-Repo` and branch `codex/restore-category-pulse-source`. A fresh checkout of this branch contains the source; the earlier task's blocked Git fetch is not needed to retrieve it.

1. Read this file, `START-HERE.md`, `SOURCE-MANIFEST.json`, and `always-on-insight-engine-prd.md`. In START-HERE, the archive extraction instruction is historical and already complete.
2. Run `cd category-pulse`. Read its README, package.json, configuration, both data files, app/API routes, and database schema/migrations.
3. Validate the restored files against the root manifest before editing. It records 115 original source files from commit `1e3673a34e5ee80f0f52737ecfdcf78a0441467e`. It describes a source snapshot, not Git object history.
4. Implement the PRD in this existing React application. Preserve source IDs, research history, user records, and existing opportunity IDs. Add the cat bundle hypothesis without replacing pantry. Reuse the earlier demo's improvements if available; otherwise implement the requirements directly in the recovered app and report that distinction.
5. Install dependencies and run development/build commands from `category-pulse/`, using the checked-in package scripts and lockfile. Root-level package/config files belong to the flattened upload; do not run the application there.
6. Complete and verify the UI, local persistence, scenario separation and guided demo. Use the supported portable local setup described in START-HERE. Do not present mocked tests as live research or deployed authentication.
7. Create a reviewable PR with an accessible preview if supported, the checks performed, and any precise remaining dependencies.

## Preserve the existing project

- Existing Site project: `appgprj_6aa33db3640c8191a61aeb4ed6eab598`.
- Keep `category-pulse/.openai/hosting.json`, both evidence files, IDs and history intact.
- Preserve the root uploads and any prior demo work. This recovery adds the proper source layout without deleting or modifying them.
- The existing scheduler is an external ChatGPT Task. Its read-only export is `existing-research-task.json`; do not create another scheduler.
- Do not create/change credentials, invoke a live model, change Site access, or publish as part of source integration.
- Source restoration does not restore runtime database contents, deployment authentication, or the original repository's full Git history.
- Report demo behavior, local persistence, deployed persistence, and real research execution separately.

The September 11 research pass reviewed 11 readable pages and added two signals. The 60-page / 7-signal walkthrough is illustrative. Keep these separate.
